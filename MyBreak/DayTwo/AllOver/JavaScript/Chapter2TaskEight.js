const prompt = require("prompt-sync")()

let numbers = prompt("Enter number ")

let negativeNumber = 0
let positiveNumber = 0

if(numbers = -1){negativeNumber = numbers }
else if(numbers = 1){positiveNumber = numbers}
console.log("the negative numbers are " + negativeNumber + "\n and positive numbers are " + positiveNumber);